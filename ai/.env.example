# Environment Configuration for Aura AI Backend

# API Keys (You need to add these)
# CoinGecko API: Get your free API key from https://www.coingecko.com/en/api/pricing
# Note: Demo/Free tier has rate limits but is sufficient for development
COINGECKO_API_KEY=your_coingecko_api_key_here

# Snowtrace API: Get your API key from https://snowtrace.io/apis  
SNOWTRACE_API_KEY=your_snowtrace_api_key_here

# Note: Pyth Network price feeds are permissionless - no API key needed
# PYTH_API_KEY=not_required

# RPC Endpoints
AVALANCHE_RPC_URL=https://api.avax.network/ext/bc/C/rpc
AVALANCHE_FUJI_RPC_URL=https://api.avax-test.network/ext/bc/C/rpc

# API Configuration
API_HOST=0.0.0.0
API_PORT=8000
DEBUG=True

# Model Configuration
MODEL_RETRAIN_INTERVAL=3600  # 1 hour in seconds
VOLATILITY_THRESHOLD=3.0     # Percentage threshold for fee adjustment
BASE_FEE_RATE=0.3           # Base fee rate in percentage

# Cache Configuration
CACHE_TTL=300               # 5 minutes cache for oracle data

# Logging
LOG_LEVEL=INFO
LOG_FORMAT=%(asctime)s - %(name)s - %(levelname)s - %(message)s

# CoinGecko Integration Notes:
# - Free tier: 30 calls/minute, 10,000 calls/month
# - Demo tier: 50 calls/minute, 50,000 calls/month  
# - Pro tier: 500 calls/minute, unlimited calls
# - The system gracefully handles rate limits with caching and fallbacks